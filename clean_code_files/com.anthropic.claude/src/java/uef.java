import kotlinx.serialization.KSerializer;
import com.anthropic.claude.api.model.ModelSection;

public final class uef extends ae9
{
    public static final uef e;
    
    static {
        ModelSection.Companion.getClass();
        e = (uef)new ae9((KSerializer)ModelSection.v.getValue(), (Enum)ModelSection.y);
    }
}
