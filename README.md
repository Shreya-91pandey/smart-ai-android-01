# AI Migration App

This Android application has been successfully migrated from Claude API to Gemini API. 

## Project Structure
- `app/src/main/java/`: Contains all migrated Java source files.
- `app/src/main/AndroidManifest.xml`: App configuration and permissions.
- GitHub Actions is configured to automatically build and generate the debug APK.
